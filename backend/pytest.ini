[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    postgres: requires a real PostgreSQL database (e.g. pg_trgm fuzzy match); skipped on SQLite
