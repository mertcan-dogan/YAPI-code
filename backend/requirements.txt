# Yapı Backend dependencies (Section 2.1)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# Database
sqlalchemy==2.0.36
alembic==1.14.0
# psycopg 3 only. DATABASE_URL is normalized to postgresql+psycopg:// in
# app/db.py so Railway's bare postgresql:// URL also uses psycopg 3 (no psycopg2).
psycopg[binary]==3.3.4

# Auth / security
pyjwt[crypto]==2.10.1          # [crypto] pulls cryptography — required for ES256/RS256 (JWKS) verification
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
bleach==6.4.0                  # CR-002-I: XSS sanitisation of text input

# Supabase / storage
supabase==2.11.0

# AI
# 0.112.0 (current) — 0.42.0 predated the extended-thinking `thinking` parameter,
# so messages.create/stream raised TypeError when AI_AGENT_THINKING_ENABLED was on
# and took the whole agent down. agent.py also guards against this at runtime.
anthropic==0.112.0

# Email
resend==2.5.1

# Observability — error monitoring (errors-only; PII/financial data scrubbed).
# [fastapi] pulls the Starlette/FastAPI integrations. Env-gated via SENTRY_DSN.
sentry-sdk[fastapi]==2.63.0

# PDF + Excel
# CR-004-A: ReportLab replaces WeasyPrint — renders on Windows without GTK/Pango/Cairo.
reportlab==4.2.5
openpyxl==3.1.5
jinja2==3.1.5

# HTTP / utils
httpx==0.28.1
# Verify TLS against the OS trust store so outbound HTTPS works behind corporate
# proxies / AV HTTPS-scanning (e.g. Norton) that re-sign certs with a local root CA.
truststore==0.10.4
python-slugify==8.0.4
python-dateutil==2.9.0.post0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
